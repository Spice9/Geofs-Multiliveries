on:
  issues:
    types: [opened]
jobs:
  titlePrefixCheck:
    runs-on: ubuntu-latest
    steps:
      - if: ${{ github.actor }} == 'Ariakim-Taiyo'
        name: Close Issue
        uses: peter-evans/close-issue@v2
        with:
          comment: |
            Test.
